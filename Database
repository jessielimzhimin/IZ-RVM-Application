CREATE DATABASE rvmdb;
CREATE USER 'rvmadmin'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON rvmdb. * TO 'rvmadmin'@'localhost';
FLUSH PRIVILEGES
CREATE studentTable (
student_id INT PRIMARY KEY,
name VARCHAR(100) NOT NULL,
points INT
) ENGINE = INNODB;
